<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Idea</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-r from-purple-200 via-pink-200 to-red-200 flex items-center justify-center min-h-screen">
    <div class="bg-white p-10 rounded-xl shadow-2xl w-full max-w-lg transform transition-all duration-500 hover:scale-105">
        <h1 class="text-4xl font-extrabold mb-8 text-center text-gray-800">Edit Idea</h1>
        <form action="/update-idea/{{idea._id}}" method="POST">
            <div class="mb-6">
                <label for="sectorId" class="block text-lg font-medium text-gray-700">Sector:</label>
                <select id="sectorId" name="sectorId" required
                    class="block w-full mt-2 px-4 py-3 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 sm:text-base">
                    {{#each sectors}}
                        <option value="{{this._id}}" {{#ifCond this._id '==' ../idea.sectorId._id}}selected{{/ifCond}}>
                            {{this.name}}
                        </option>
                    {{/each}}
                </select>
            </div>
            <div class="mb-6">
                <label for="problemStatement" class="block text-lg font-medium text-gray-700">Problem Statement:</label>
                <textarea id="problemStatement" name="problemStatement" required 
                class="block w-full mt-2 px-4 py-3 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500 sm:text-base">{{idea.problemStatement}}</textarea>
            </div>
            <button type="submit" class="w-full bg-indigo-600 text-white py-3 rounded-md shadow-lg hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 transform transition-transform duration-200 hover:scale-105">Update Idea</button>
        </form>
        <a href="/my-ideas" class="block mt-6 text-center text-blue-600 font-medium hover:text-blue-800 hover:underline transition duration-300">Back to My Ideas</a>
    </div>
</body>
</html>
