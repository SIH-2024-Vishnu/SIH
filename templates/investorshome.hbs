<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Investor Home</title>
    <!-- Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-b from-gray-100 to-gray-300 min-h-screen flex items-center justify-center">
    <div class="container mx-auto p-8 bg-white shadow-lg rounded-lg">
        <h1 class="text-4xl font-bold mb-8 text-center text-gray-800">Welcome, Investor</h1>
        <h2 class="text-2xl font-semibold mb-6 text-center text-gray-700">Ideas from Contributors</h2>

        <ul class="space-y-6">
            {{#each ideas}}
            <li class="p-6 border border-gray-200 rounded-lg shadow-md bg-gray-50 hover:bg-gray-100 transition duration-200">
                <div class="flex justify-between mb-2">
                    <span class="font-semibold text-gray-700">Contributor:</span>
                    <span class="text-gray-800">{{this.contributorId.name}}</span>
                </div>
                <div class="flex justify-between mb-2">
                    <span class="font-semibold text-gray-700">Sector:</span>
                    <span class="text-gray-800">{{this.sectorId.name}}</span>
                </div>
                <h3 class="text-xl font-medium text-gray-900 mb-4">{{this.problemStatement}}</h3>
                <small class="text-gray-500">Posted on: {{this.datePosted}}</small>
            </li>
            {{/each}}
        </ul>
    </div>
</body>
</html>
